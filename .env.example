# Copy this to .env.dev (for dev stage) or .env.prod and fill in the values.
# These are NON-SECRET configuration values only — all secrets go in AWS Secrets Manager.

STAGE=dev
REGION=us-east-1
LOG_LEVEL=DEBUG